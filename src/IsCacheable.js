/**
 * Determinar si una respuesta HTTP se puede almacenar en la caché.
 * 
 * Obviamente, hay que desarollar este código, pasando también parámetros adecuados.
 */
function IsCacheable() {
  return true;
}

module.exports = IsCacheable;